# STATUS — doctorlaser

> Repo kia đọc file này để biết bên này đang làm gì. Cập nhật mỗi lượt làm việc.
> Đọc trạng thái bên kia: `./scripts/team.sh`

| | |
|---|---|
| Cập nhật | 2026-08-13 |
| Nhánh | `claude/manage-control-two-processes-p7gf7q` |
| Commit gần nhất | `6a45b25` |
| Chạy bằng | `Procfile → worker: python bot.py` (Railway) |
| Tình trạng | Đang chạy thật |

## Đang làm

- Không có việc đang dở.

## Vừa xong (mới nhất trước)

1. `6a45b25` — TikTok: báo cáo ngày tự nhắc trước khi refresh token hết hạn (≤5 ngày).
2. `ac098c4` — TikTok: bot tự refresh token khi gặp 401 / 40105 / 40001.
3. `eec18c9` — Báo cáo lũy kế: thêm dòng tổng chi ads (FB + TikTok) và % doanh thu.

## Đang chờ bên kia (`kiot`)

- Chưa có. `kiot` chưa nhận việc.

## Cảnh báo cho bên kia

- `kiotviet.py` và `sales.py` hiện **vẫn nằm ở repo này**. Nếu `kiot` định nhận mảng
  KiotViet thì phải chốt ranh giới trong `TEAM.md` trước, tránh 2 bên cùng sửa.

## Việc quản lý tổng giao

- (trống)
